# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartfitgirl
DB_USER=smartfit
DB_PASSWORD=smartfit123

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Service Configuration
# Internal container port (usually 8080 for all Go services)
SERVICE_PORT=8080

# External host ports for local development (Docker Desktop)
# These ports are mapped in docker-compose.yml for external access
API_SERVICE_HOST_PORT=8080
USER_SERVICE_HOST_PORT=8082
MEAL_SERVICE_HOST_PORT=8083
TRACKING_SERVICE_HOST_PORT=8084
WEB_CLIENT_HOST_PORT=5050
POSTGRES_HOST_PORT=5432

# Service URLs for inter-service communication (Docker network)
# These use service names and internal ports
USER_SERVICE_URL=http://user-service:8080
MEAL_SERVICE_URL=http://meal-service:8080
TRACKING_SERVICE_URL=http://tracking-service:8080

# React App Environment (PostHog only in client)
REACT_APP_API_URL=http://localhost:8080
REACT_APP_POSTHOG_KEY=your-posthog-api-key
REACT_APP_POSTHOG_HOST=https://app.posthog.com
